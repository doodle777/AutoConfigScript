#!/bin/bash
##安装常用软件

# Install Vim
sudo apt install vim

# Install openjdk-8
sudo apt install openjdk-8-jdk

# Install psense
sudo apt install psense

# Install xclip
sudo apt install xclip

# Add USB device support with exfat format.
sudo apt install exfat-utils

# Install screenfetch
sudo apt install screenfetch
